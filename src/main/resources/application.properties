server.port=${PORT:8080}

# PostgreSQL Configuration (use env vars for production)
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://db.aamzezukxjcnwoostttd.supabase.co:5432/postgres?sslmode=require}
spring.datasource.username=${DATABASE_USERNAME:postgres}
spring.datasource.password=${DATABASE_PASSWORD:Trungkienak@123}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=${HIKARI_MAX_POOL:5}

# JPA Configuration
spring.jpa.hibernate.ddl-auto=${DDL_AUTO:update}
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.defer-datasource-initialization=false

# CORS
app.cors.allowed-origins=${CORS_ORIGINS:http://localhost:3000,http://localhost:5173,https://dodongkimanh.vercel.app}

# JWT Configuration
app.jwt.secret=${JWT_SECRET:KimAnhCRM2024SecretKeyForJWTAuthenticationSystemVeryLongAndSecure256Bits!!}
app.jwt.expiration=${JWT_EXPIRATION:86400000}

# Redis (Upstash) Configuration
spring.data.redis.host=${REDIS_HOST:blessed-cat-73757.upstash.io}
spring.data.redis.port=${REDIS_PORT:6379}
spring.data.redis.password=${REDIS_PASSWORD:gQAAAAAAASAdAAIncDI1Zjc1YjY1N2FjODE0NTJkYTMxZDI3NGY3YzJjZWI2YXAyNzM3NTc}
spring.data.redis.ssl.enabled=${REDIS_SSL:true}
